// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: shop/shop_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

/** Enums */
export enum ShopType {
  SHOP_TYPE_UNSPECIFIED = 0,
  SHOP_TYPE_FASHION = 1,
  UNRECOGNIZED = -1,
}

export function shopTypeFromJSON(object: any): ShopType {
  switch (object) {
    case 0:
    case "SHOP_TYPE_UNSPECIFIED":
      return ShopType.SHOP_TYPE_UNSPECIFIED;
    case 1:
    case "SHOP_TYPE_FASHION":
      return ShopType.SHOP_TYPE_FASHION;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ShopType.UNRECOGNIZED;
  }
}

export function shopTypeToJSON(object: ShopType): string {
  switch (object) {
    case ShopType.SHOP_TYPE_UNSPECIFIED:
      return "SHOP_TYPE_UNSPECIFIED";
    case ShopType.SHOP_TYPE_FASHION:
      return "SHOP_TYPE_FASHION";
    case ShopType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ShopSortBy {
  SHOP_SORT_BY_UNSPECIFIED = 0,
  SHOP_SORT_BY_UPDATED_AT = 1,
  SHOP_SORT_BY_NAME = 2,
  UNRECOGNIZED = -1,
}

export function shopSortByFromJSON(object: any): ShopSortBy {
  switch (object) {
    case 0:
    case "SHOP_SORT_BY_UNSPECIFIED":
      return ShopSortBy.SHOP_SORT_BY_UNSPECIFIED;
    case 1:
    case "SHOP_SORT_BY_UPDATED_AT":
      return ShopSortBy.SHOP_SORT_BY_UPDATED_AT;
    case 2:
    case "SHOP_SORT_BY_NAME":
      return ShopSortBy.SHOP_SORT_BY_NAME;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ShopSortBy.UNRECOGNIZED;
  }
}

export function shopSortByToJSON(object: ShopSortBy): string {
  switch (object) {
    case ShopSortBy.SHOP_SORT_BY_UNSPECIFIED:
      return "SHOP_SORT_BY_UNSPECIFIED";
    case ShopSortBy.SHOP_SORT_BY_UPDATED_AT:
      return "SHOP_SORT_BY_UPDATED_AT";
    case ShopSortBy.SHOP_SORT_BY_NAME:
      return "SHOP_SORT_BY_NAME";
    case ShopSortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ShopUserType {
  SHOP_USER_TYPE_OWNER = 0,
  UNRECOGNIZED = -1,
}

export function shopUserTypeFromJSON(object: any): ShopUserType {
  switch (object) {
    case 0:
    case "SHOP_USER_TYPE_OWNER":
      return ShopUserType.SHOP_USER_TYPE_OWNER;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ShopUserType.UNRECOGNIZED;
  }
}

export function shopUserTypeToJSON(object: ShopUserType): string {
  switch (object) {
    case ShopUserType.SHOP_USER_TYPE_OWNER:
      return "SHOP_USER_TYPE_OWNER";
    case ShopUserType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Create Shop */
export interface CreateShopRequest {
  name: string;
  type: ShopType;
}

export interface CreateShopResponse {
  shopId: string;
}

/** Get Shops */
export interface GetShopsRequest {
  sortBy: ShopSortBy;
  types: ShopType[];
  activeOnly: boolean;
  pageSize: number;
  pageIndex: number;
  sortDescending: boolean;
  searchTerm: string;
}

export interface GetShopsResponse {
  items: ShopData[];
  totalResults: number;
  pageSize: number;
  pageIndex: number;
  maxPageIndex: number;
}

/** Shop */
export interface ShopData {
  id: string;
  name: string;
  type: ShopType;
  isActive: boolean;
  updatedAt?: string | undefined;
}

export interface GetShopMemberRequest {
  shopId: string;
  userId: string;
}

export interface GetShopMemberResponse {
  shopMember?: ShopMemberData | undefined;
}

export interface ShopMemberData {
  shopId: string;
  userId: string;
  shopUserType: ShopUserType;
}

function createBaseCreateShopRequest(): CreateShopRequest {
  return { name: "", type: 0 };
}

export const CreateShopRequest: MessageFns<CreateShopRequest> = {
  encode(message: CreateShopRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateShopRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateShopRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateShopRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? shopTypeFromJSON(object.type) : 0,
    };
  },

  toJSON(message: CreateShopRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = shopTypeToJSON(message.type);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateShopRequest>, I>>(base?: I): CreateShopRequest {
    return CreateShopRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateShopRequest>, I>>(object: I): CreateShopRequest {
    const message = createBaseCreateShopRequest();
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    return message;
  },
};

function createBaseCreateShopResponse(): CreateShopResponse {
  return { shopId: "" };
}

export const CreateShopResponse: MessageFns<CreateShopResponse> = {
  encode(message: CreateShopResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopId !== "") {
      writer.uint32(10).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateShopResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateShopResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateShopResponse {
    return { shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "" };
  },

  toJSON(message: CreateShopResponse): unknown {
    const obj: any = {};
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateShopResponse>, I>>(base?: I): CreateShopResponse {
    return CreateShopResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateShopResponse>, I>>(object: I): CreateShopResponse {
    const message = createBaseCreateShopResponse();
    message.shopId = object.shopId ?? "";
    return message;
  },
};

function createBaseGetShopsRequest(): GetShopsRequest {
  return { sortBy: 0, types: [], activeOnly: false, pageSize: 0, pageIndex: 0, sortDescending: false, searchTerm: "" };
}

export const GetShopsRequest: MessageFns<GetShopsRequest> = {
  encode(message: GetShopsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sortBy !== 0) {
      writer.uint32(8).int32(message.sortBy);
    }
    writer.uint32(18).fork();
    for (const v of message.types) {
      writer.int32(v);
    }
    writer.join();
    if (message.activeOnly !== false) {
      writer.uint32(24).bool(message.activeOnly);
    }
    if (message.pageSize !== 0) {
      writer.uint32(32).int32(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      writer.uint32(40).int32(message.pageIndex);
    }
    if (message.sortDescending !== false) {
      writer.uint32(48).bool(message.sortDescending);
    }
    if (message.searchTerm !== "") {
      writer.uint32(58).string(message.searchTerm);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetShopsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetShopsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 2: {
          if (tag === 16) {
            message.types.push(reader.int32() as any);

            continue;
          }

          if (tag === 18) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.types.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.activeOnly = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 48) {
            break;
          }

          message.sortDescending = reader.bool();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.searchTerm = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetShopsRequest {
    return {
      sortBy: isSet(object.sortBy) ? shopSortByFromJSON(object.sortBy) : 0,
      types: globalThis.Array.isArray(object?.types) ? object.types.map((e: any) => shopTypeFromJSON(e)) : [],
      activeOnly: isSet(object.activeOnly) ? globalThis.Boolean(object.activeOnly) : false,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      sortDescending: isSet(object.sortDescending) ? globalThis.Boolean(object.sortDescending) : false,
      searchTerm: isSet(object.searchTerm) ? globalThis.String(object.searchTerm) : "",
    };
  },

  toJSON(message: GetShopsRequest): unknown {
    const obj: any = {};
    if (message.sortBy !== 0) {
      obj.sortBy = shopSortByToJSON(message.sortBy);
    }
    if (message.types?.length) {
      obj.types = message.types.map((e) => shopTypeToJSON(e));
    }
    if (message.activeOnly !== false) {
      obj.activeOnly = message.activeOnly;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.sortDescending !== false) {
      obj.sortDescending = message.sortDescending;
    }
    if (message.searchTerm !== "") {
      obj.searchTerm = message.searchTerm;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetShopsRequest>, I>>(base?: I): GetShopsRequest {
    return GetShopsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetShopsRequest>, I>>(object: I): GetShopsRequest {
    const message = createBaseGetShopsRequest();
    message.sortBy = object.sortBy ?? 0;
    message.types = object.types?.map((e) => e) || [];
    message.activeOnly = object.activeOnly ?? false;
    message.pageSize = object.pageSize ?? 0;
    message.pageIndex = object.pageIndex ?? 0;
    message.sortDescending = object.sortDescending ?? false;
    message.searchTerm = object.searchTerm ?? "";
    return message;
  },
};

function createBaseGetShopsResponse(): GetShopsResponse {
  return { items: [], totalResults: 0, pageSize: 0, pageIndex: 0, maxPageIndex: 0 };
}

export const GetShopsResponse: MessageFns<GetShopsResponse> = {
  encode(message: GetShopsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ShopData.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalResults !== 0) {
      writer.uint32(16).int64(message.totalResults);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      writer.uint32(32).int32(message.pageIndex);
    }
    if (message.maxPageIndex !== 0) {
      writer.uint32(40).int32(message.maxPageIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetShopsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetShopsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ShopData.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalResults = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxPageIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetShopsResponse {
    return {
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => ShopData.fromJSON(e)) : [],
      totalResults: isSet(object.totalResults) ? globalThis.Number(object.totalResults) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      maxPageIndex: isSet(object.maxPageIndex) ? globalThis.Number(object.maxPageIndex) : 0,
    };
  },

  toJSON(message: GetShopsResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ShopData.toJSON(e));
    }
    if (message.totalResults !== 0) {
      obj.totalResults = Math.round(message.totalResults);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.maxPageIndex !== 0) {
      obj.maxPageIndex = Math.round(message.maxPageIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetShopsResponse>, I>>(base?: I): GetShopsResponse {
    return GetShopsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetShopsResponse>, I>>(object: I): GetShopsResponse {
    const message = createBaseGetShopsResponse();
    message.items = object.items?.map((e) => ShopData.fromPartial(e)) || [];
    message.totalResults = object.totalResults ?? 0;
    message.pageSize = object.pageSize ?? 0;
    message.pageIndex = object.pageIndex ?? 0;
    message.maxPageIndex = object.maxPageIndex ?? 0;
    return message;
  },
};

function createBaseShopData(): ShopData {
  return { id: "", name: "", type: 0, isActive: false, updatedAt: undefined };
}

export const ShopData: MessageFns<ShopData> = {
  encode(message: ShopData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.isActive !== false) {
      writer.uint32(32).bool(message.isActive);
    }
    if (message.updatedAt !== undefined) {
      writer.uint32(42).string(message.updatedAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShopData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShopData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.isActive = reader.bool();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShopData {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? shopTypeFromJSON(object.type) : 0,
      isActive: isSet(object.isActive) ? globalThis.Boolean(object.isActive) : false,
      updatedAt: isSet(object.updatedAt) ? globalThis.String(object.updatedAt) : undefined,
    };
  },

  toJSON(message: ShopData): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = shopTypeToJSON(message.type);
    }
    if (message.isActive !== false) {
      obj.isActive = message.isActive;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShopData>, I>>(base?: I): ShopData {
    return ShopData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShopData>, I>>(object: I): ShopData {
    const message = createBaseShopData();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.isActive = object.isActive ?? false;
    message.updatedAt = object.updatedAt ?? undefined;
    return message;
  },
};

function createBaseGetShopMemberRequest(): GetShopMemberRequest {
  return { shopId: "", userId: "" };
}

export const GetShopMemberRequest: MessageFns<GetShopMemberRequest> = {
  encode(message: GetShopMemberRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopId !== "") {
      writer.uint32(10).string(message.shopId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetShopMemberRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetShopMemberRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetShopMemberRequest {
    return {
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
    };
  },

  toJSON(message: GetShopMemberRequest): unknown {
    const obj: any = {};
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetShopMemberRequest>, I>>(base?: I): GetShopMemberRequest {
    return GetShopMemberRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetShopMemberRequest>, I>>(object: I): GetShopMemberRequest {
    const message = createBaseGetShopMemberRequest();
    message.shopId = object.shopId ?? "";
    message.userId = object.userId ?? "";
    return message;
  },
};

function createBaseGetShopMemberResponse(): GetShopMemberResponse {
  return { shopMember: undefined };
}

export const GetShopMemberResponse: MessageFns<GetShopMemberResponse> = {
  encode(message: GetShopMemberResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopMember !== undefined) {
      ShopMemberData.encode(message.shopMember, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetShopMemberResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetShopMemberResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopMember = ShopMemberData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetShopMemberResponse {
    return { shopMember: isSet(object.shopMember) ? ShopMemberData.fromJSON(object.shopMember) : undefined };
  },

  toJSON(message: GetShopMemberResponse): unknown {
    const obj: any = {};
    if (message.shopMember !== undefined) {
      obj.shopMember = ShopMemberData.toJSON(message.shopMember);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetShopMemberResponse>, I>>(base?: I): GetShopMemberResponse {
    return GetShopMemberResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetShopMemberResponse>, I>>(object: I): GetShopMemberResponse {
    const message = createBaseGetShopMemberResponse();
    message.shopMember = (object.shopMember !== undefined && object.shopMember !== null)
      ? ShopMemberData.fromPartial(object.shopMember)
      : undefined;
    return message;
  },
};

function createBaseShopMemberData(): ShopMemberData {
  return { shopId: "", userId: "", shopUserType: 0 };
}

export const ShopMemberData: MessageFns<ShopMemberData> = {
  encode(message: ShopMemberData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopId !== "") {
      writer.uint32(10).string(message.shopId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.shopUserType !== 0) {
      writer.uint32(24).int32(message.shopUserType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ShopMemberData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseShopMemberData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.shopUserType = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ShopMemberData {
    return {
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      shopUserType: isSet(object.shopUserType) ? shopUserTypeFromJSON(object.shopUserType) : 0,
    };
  },

  toJSON(message: ShopMemberData): unknown {
    const obj: any = {};
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.shopUserType !== 0) {
      obj.shopUserType = shopUserTypeToJSON(message.shopUserType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ShopMemberData>, I>>(base?: I): ShopMemberData {
    return ShopMemberData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ShopMemberData>, I>>(object: I): ShopMemberData {
    const message = createBaseShopMemberData();
    message.shopId = object.shopId ?? "";
    message.userId = object.userId ?? "";
    message.shopUserType = object.shopUserType ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
