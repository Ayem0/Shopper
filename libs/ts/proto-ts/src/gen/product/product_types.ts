// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: product/product_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { ActiveStatus, activeStatusFromJSON, activeStatusToJSON } from "../common/common_types";

export enum ProductSortBy {
  PRODUCT_SORT_BY_UNSPECIFIED = 0,
  PRODUCT_SORT_BY_NAME = 1,
  PRODUCT_SORT_BY_UPDATED_AT = 2,
  UNRECOGNIZED = -1,
}

export function productSortByFromJSON(object: any): ProductSortBy {
  switch (object) {
    case 0:
    case "PRODUCT_SORT_BY_UNSPECIFIED":
      return ProductSortBy.PRODUCT_SORT_BY_UNSPECIFIED;
    case 1:
    case "PRODUCT_SORT_BY_NAME":
      return ProductSortBy.PRODUCT_SORT_BY_NAME;
    case 2:
    case "PRODUCT_SORT_BY_UPDATED_AT":
      return ProductSortBy.PRODUCT_SORT_BY_UPDATED_AT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProductSortBy.UNRECOGNIZED;
  }
}

export function productSortByToJSON(object: ProductSortBy): string {
  switch (object) {
    case ProductSortBy.PRODUCT_SORT_BY_UNSPECIFIED:
      return "PRODUCT_SORT_BY_UNSPECIFIED";
    case ProductSortBy.PRODUCT_SORT_BY_NAME:
      return "PRODUCT_SORT_BY_NAME";
    case ProductSortBy.PRODUCT_SORT_BY_UPDATED_AT:
      return "PRODUCT_SORT_BY_UPDATED_AT";
    case ProductSortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum ProductCategorySortBy {
  PRODUCT_CATEGORY_SORT_BY_UNSPECIFIED = 0,
  PRODUCT_CATEGORY_SORT_BY_NAME = 1,
  PRODUCT_CATEGORY_SORT_BY_UPDATED_AT = 2,
  UNRECOGNIZED = -1,
}

export function productCategorySortByFromJSON(object: any): ProductCategorySortBy {
  switch (object) {
    case 0:
    case "PRODUCT_CATEGORY_SORT_BY_UNSPECIFIED":
      return ProductCategorySortBy.PRODUCT_CATEGORY_SORT_BY_UNSPECIFIED;
    case 1:
    case "PRODUCT_CATEGORY_SORT_BY_NAME":
      return ProductCategorySortBy.PRODUCT_CATEGORY_SORT_BY_NAME;
    case 2:
    case "PRODUCT_CATEGORY_SORT_BY_UPDATED_AT":
      return ProductCategorySortBy.PRODUCT_CATEGORY_SORT_BY_UPDATED_AT;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ProductCategorySortBy.UNRECOGNIZED;
  }
}

export function productCategorySortByToJSON(object: ProductCategorySortBy): string {
  switch (object) {
    case ProductCategorySortBy.PRODUCT_CATEGORY_SORT_BY_UNSPECIFIED:
      return "PRODUCT_CATEGORY_SORT_BY_UNSPECIFIED";
    case ProductCategorySortBy.PRODUCT_CATEGORY_SORT_BY_NAME:
      return "PRODUCT_CATEGORY_SORT_BY_NAME";
    case ProductCategorySortBy.PRODUCT_CATEGORY_SORT_BY_UPDATED_AT:
      return "PRODUCT_CATEGORY_SORT_BY_UPDATED_AT";
    case ProductCategorySortBy.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface ProductData {
  id: string;
  name: string;
  descr?: string | undefined;
  categories: ProductCategoryData[];
  status: ActiveStatus;
  shopId: string;
}

export interface ProductVariantData {
  id: string;
  shopId: string;
  name: string;
  status: ActiveStatus;
  variantOptionValues: VariantOptionValueData[];
  product?: ProductData | undefined;
}

export interface VariantOptionValueData {
  id: string;
  shopId: string;
  name: string;
  value: string;
  optionValueId: string;
}

export interface VariantOption {
  name: string;
  values: string[];
}

export interface CreateProductRequest {
  name: string;
  descr?: string | undefined;
  categoryIds: string[];
  variantOptions: VariantOption[];
  status: ActiveStatus;
  shopId: string;
}

export interface CreateProductResponse {
  productId: string;
}

export interface GetProductsRequest {
  search: string;
  sortBy: ProductSortBy;
  desc: boolean;
  pageSize: number;
  pageIndex: number;
  status: ActiveStatus[];
  shopId: string;
}

export interface GetProductsResponse {
  items: ProductVariantData[];
  totalResults: number;
  pageSize: number;
  pageIndex: number;
  maxPageIndex: number;
}

/** Category */
export interface ProductCategoryData {
  id: string;
  name: string;
  status: ActiveStatus;
  parentCategory?: ProductCategoryData | undefined;
  parentCategoryId?: string | undefined;
  updatedAt?: string | undefined;
  shopId: string;
}

export interface CreateProductCategoryRequest {
  shopId: string;
  name: string;
  status: ActiveStatus;
  parentCategoryId?: string | undefined;
}

export interface CreateProductCategoryResponse {
  categoryId: string;
}

export interface GetProductCategoriesRequest {
  search: string;
  sortBy: ProductCategorySortBy;
  desc: boolean;
  pageSize: number;
  pageIndex: number;
  status: ActiveStatus[];
  shopId: string;
}

export interface GetProductCategoriesResponse {
  items: ProductCategoryData[];
  totalResults: number;
  pageSize: number;
  pageIndex: number;
  maxPageIndex: number;
}

function createBaseProductData(): ProductData {
  return { id: "", name: "", descr: undefined, categories: [], status: 0, shopId: "" };
}

export const ProductData: MessageFns<ProductData> = {
  encode(message: ProductData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.descr !== undefined) {
      writer.uint32(26).string(message.descr);
    }
    for (const v of message.categories) {
      ProductCategoryData.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.shopId !== "") {
      writer.uint32(50).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProductData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProductData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.descr = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.categories.push(ProductCategoryData.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProductData {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      descr: isSet(object.descr) ? globalThis.String(object.descr) : undefined,
      categories: globalThis.Array.isArray(object?.categories)
        ? object.categories.map((e: any) => ProductCategoryData.fromJSON(e))
        : [],
      status: isSet(object.status) ? activeStatusFromJSON(object.status) : 0,
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
    };
  },

  toJSON(message: ProductData): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.descr !== undefined) {
      obj.descr = message.descr;
    }
    if (message.categories?.length) {
      obj.categories = message.categories.map((e) => ProductCategoryData.toJSON(e));
    }
    if (message.status !== 0) {
      obj.status = activeStatusToJSON(message.status);
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProductData>, I>>(base?: I): ProductData {
    return ProductData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProductData>, I>>(object: I): ProductData {
    const message = createBaseProductData();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.descr = object.descr ?? undefined;
    message.categories = object.categories?.map((e) => ProductCategoryData.fromPartial(e)) || [];
    message.status = object.status ?? 0;
    message.shopId = object.shopId ?? "";
    return message;
  },
};

function createBaseProductVariantData(): ProductVariantData {
  return { id: "", shopId: "", name: "", status: 0, variantOptionValues: [], product: undefined };
}

export const ProductVariantData: MessageFns<ProductVariantData> = {
  encode(message: ProductVariantData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.shopId !== "") {
      writer.uint32(18).string(message.shopId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.status !== 0) {
      writer.uint32(32).int32(message.status);
    }
    for (const v of message.variantOptionValues) {
      VariantOptionValueData.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.product !== undefined) {
      ProductData.encode(message.product, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProductVariantData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProductVariantData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.variantOptionValues.push(VariantOptionValueData.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.product = ProductData.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProductVariantData {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      status: isSet(object.status) ? activeStatusFromJSON(object.status) : 0,
      variantOptionValues: globalThis.Array.isArray(object?.variantOptionValues)
        ? object.variantOptionValues.map((e: any) => VariantOptionValueData.fromJSON(e))
        : [],
      product: isSet(object.product) ? ProductData.fromJSON(object.product) : undefined,
    };
  },

  toJSON(message: ProductVariantData): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.status !== 0) {
      obj.status = activeStatusToJSON(message.status);
    }
    if (message.variantOptionValues?.length) {
      obj.variantOptionValues = message.variantOptionValues.map((e) => VariantOptionValueData.toJSON(e));
    }
    if (message.product !== undefined) {
      obj.product = ProductData.toJSON(message.product);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProductVariantData>, I>>(base?: I): ProductVariantData {
    return ProductVariantData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProductVariantData>, I>>(object: I): ProductVariantData {
    const message = createBaseProductVariantData();
    message.id = object.id ?? "";
    message.shopId = object.shopId ?? "";
    message.name = object.name ?? "";
    message.status = object.status ?? 0;
    message.variantOptionValues = object.variantOptionValues?.map((e) => VariantOptionValueData.fromPartial(e)) || [];
    message.product = (object.product !== undefined && object.product !== null)
      ? ProductData.fromPartial(object.product)
      : undefined;
    return message;
  },
};

function createBaseVariantOptionValueData(): VariantOptionValueData {
  return { id: "", shopId: "", name: "", value: "", optionValueId: "" };
}

export const VariantOptionValueData: MessageFns<VariantOptionValueData> = {
  encode(message: VariantOptionValueData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.shopId !== "") {
      writer.uint32(18).string(message.shopId);
    }
    if (message.name !== "") {
      writer.uint32(26).string(message.name);
    }
    if (message.value !== "") {
      writer.uint32(34).string(message.value);
    }
    if (message.optionValueId !== "") {
      writer.uint32(42).string(message.optionValueId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VariantOptionValueData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVariantOptionValueData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.value = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.optionValueId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VariantOptionValueData {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
      optionValueId: isSet(object.optionValueId) ? globalThis.String(object.optionValueId) : "",
    };
  },

  toJSON(message: VariantOptionValueData): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    if (message.optionValueId !== "") {
      obj.optionValueId = message.optionValueId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VariantOptionValueData>, I>>(base?: I): VariantOptionValueData {
    return VariantOptionValueData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VariantOptionValueData>, I>>(object: I): VariantOptionValueData {
    const message = createBaseVariantOptionValueData();
    message.id = object.id ?? "";
    message.shopId = object.shopId ?? "";
    message.name = object.name ?? "";
    message.value = object.value ?? "";
    message.optionValueId = object.optionValueId ?? "";
    return message;
  },
};

function createBaseVariantOption(): VariantOption {
  return { name: "", values: [] };
}

export const VariantOption: MessageFns<VariantOption> = {
  encode(message: VariantOption, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.values) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VariantOption {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVariantOption();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.values.push(reader.string());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VariantOption {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      values: globalThis.Array.isArray(object?.values) ? object.values.map((e: any) => globalThis.String(e)) : [],
    };
  },

  toJSON(message: VariantOption): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.values?.length) {
      obj.values = message.values;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<VariantOption>, I>>(base?: I): VariantOption {
    return VariantOption.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<VariantOption>, I>>(object: I): VariantOption {
    const message = createBaseVariantOption();
    message.name = object.name ?? "";
    message.values = object.values?.map((e) => e) || [];
    return message;
  },
};

function createBaseCreateProductRequest(): CreateProductRequest {
  return { name: "", descr: undefined, categoryIds: [], variantOptions: [], status: 0, shopId: "" };
}

export const CreateProductRequest: MessageFns<CreateProductRequest> = {
  encode(message: CreateProductRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.descr !== undefined) {
      writer.uint32(18).string(message.descr);
    }
    for (const v of message.categoryIds) {
      writer.uint32(26).string(v!);
    }
    for (const v of message.variantOptions) {
      VariantOption.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.shopId !== "") {
      writer.uint32(50).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.descr = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.categoryIds.push(reader.string());
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.variantOptions.push(VariantOption.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductRequest {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      descr: isSet(object.descr) ? globalThis.String(object.descr) : undefined,
      categoryIds: globalThis.Array.isArray(object?.categoryIds)
        ? object.categoryIds.map((e: any) => globalThis.String(e))
        : [],
      variantOptions: globalThis.Array.isArray(object?.variantOptions)
        ? object.variantOptions.map((e: any) => VariantOption.fromJSON(e))
        : [],
      status: isSet(object.status) ? activeStatusFromJSON(object.status) : 0,
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
    };
  },

  toJSON(message: CreateProductRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.descr !== undefined) {
      obj.descr = message.descr;
    }
    if (message.categoryIds?.length) {
      obj.categoryIds = message.categoryIds;
    }
    if (message.variantOptions?.length) {
      obj.variantOptions = message.variantOptions.map((e) => VariantOption.toJSON(e));
    }
    if (message.status !== 0) {
      obj.status = activeStatusToJSON(message.status);
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductRequest>, I>>(base?: I): CreateProductRequest {
    return CreateProductRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductRequest>, I>>(object: I): CreateProductRequest {
    const message = createBaseCreateProductRequest();
    message.name = object.name ?? "";
    message.descr = object.descr ?? undefined;
    message.categoryIds = object.categoryIds?.map((e) => e) || [];
    message.variantOptions = object.variantOptions?.map((e) => VariantOption.fromPartial(e)) || [];
    message.status = object.status ?? 0;
    message.shopId = object.shopId ?? "";
    return message;
  },
};

function createBaseCreateProductResponse(): CreateProductResponse {
  return { productId: "" };
}

export const CreateProductResponse: MessageFns<CreateProductResponse> = {
  encode(message: CreateProductResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.productId !== "") {
      writer.uint32(10).string(message.productId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.productId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductResponse {
    return { productId: isSet(object.productId) ? globalThis.String(object.productId) : "" };
  },

  toJSON(message: CreateProductResponse): unknown {
    const obj: any = {};
    if (message.productId !== "") {
      obj.productId = message.productId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductResponse>, I>>(base?: I): CreateProductResponse {
    return CreateProductResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductResponse>, I>>(object: I): CreateProductResponse {
    const message = createBaseCreateProductResponse();
    message.productId = object.productId ?? "";
    return message;
  },
};

function createBaseGetProductsRequest(): GetProductsRequest {
  return { search: "", sortBy: 0, desc: false, pageSize: 0, pageIndex: 0, status: [], shopId: "" };
}

export const GetProductsRequest: MessageFns<GetProductsRequest> = {
  encode(message: GetProductsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.search !== "") {
      writer.uint32(10).string(message.search);
    }
    if (message.sortBy !== 0) {
      writer.uint32(16).int32(message.sortBy);
    }
    if (message.desc !== false) {
      writer.uint32(24).bool(message.desc);
    }
    if (message.pageSize !== 0) {
      writer.uint32(32).int32(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      writer.uint32(40).int32(message.pageIndex);
    }
    writer.uint32(50).fork();
    for (const v of message.status) {
      writer.int32(v);
    }
    writer.join();
    if (message.shopId !== "") {
      writer.uint32(58).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProductsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProductsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.search = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.desc = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.status.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.status.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProductsRequest {
    return {
      search: isSet(object.search) ? globalThis.String(object.search) : "",
      sortBy: isSet(object.sortBy) ? productSortByFromJSON(object.sortBy) : 0,
      desc: isSet(object.desc) ? globalThis.Boolean(object.desc) : false,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      status: globalThis.Array.isArray(object?.status) ? object.status.map((e: any) => activeStatusFromJSON(e)) : [],
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
    };
  },

  toJSON(message: GetProductsRequest): unknown {
    const obj: any = {};
    if (message.search !== "") {
      obj.search = message.search;
    }
    if (message.sortBy !== 0) {
      obj.sortBy = productSortByToJSON(message.sortBy);
    }
    if (message.desc !== false) {
      obj.desc = message.desc;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.status?.length) {
      obj.status = message.status.map((e) => activeStatusToJSON(e));
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProductsRequest>, I>>(base?: I): GetProductsRequest {
    return GetProductsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProductsRequest>, I>>(object: I): GetProductsRequest {
    const message = createBaseGetProductsRequest();
    message.search = object.search ?? "";
    message.sortBy = object.sortBy ?? 0;
    message.desc = object.desc ?? false;
    message.pageSize = object.pageSize ?? 0;
    message.pageIndex = object.pageIndex ?? 0;
    message.status = object.status?.map((e) => e) || [];
    message.shopId = object.shopId ?? "";
    return message;
  },
};

function createBaseGetProductsResponse(): GetProductsResponse {
  return { items: [], totalResults: 0, pageSize: 0, pageIndex: 0, maxPageIndex: 0 };
}

export const GetProductsResponse: MessageFns<GetProductsResponse> = {
  encode(message: GetProductsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ProductVariantData.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalResults !== 0) {
      writer.uint32(16).int64(message.totalResults);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      writer.uint32(32).int32(message.pageIndex);
    }
    if (message.maxPageIndex !== 0) {
      writer.uint32(40).int32(message.maxPageIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProductsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProductsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ProductVariantData.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalResults = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxPageIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProductsResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ProductVariantData.fromJSON(e))
        : [],
      totalResults: isSet(object.totalResults) ? globalThis.Number(object.totalResults) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      maxPageIndex: isSet(object.maxPageIndex) ? globalThis.Number(object.maxPageIndex) : 0,
    };
  },

  toJSON(message: GetProductsResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ProductVariantData.toJSON(e));
    }
    if (message.totalResults !== 0) {
      obj.totalResults = Math.round(message.totalResults);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.maxPageIndex !== 0) {
      obj.maxPageIndex = Math.round(message.maxPageIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProductsResponse>, I>>(base?: I): GetProductsResponse {
    return GetProductsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProductsResponse>, I>>(object: I): GetProductsResponse {
    const message = createBaseGetProductsResponse();
    message.items = object.items?.map((e) => ProductVariantData.fromPartial(e)) || [];
    message.totalResults = object.totalResults ?? 0;
    message.pageSize = object.pageSize ?? 0;
    message.pageIndex = object.pageIndex ?? 0;
    message.maxPageIndex = object.maxPageIndex ?? 0;
    return message;
  },
};

function createBaseProductCategoryData(): ProductCategoryData {
  return {
    id: "",
    name: "",
    status: 0,
    parentCategory: undefined,
    parentCategoryId: undefined,
    updatedAt: undefined,
    shopId: "",
  };
}

export const ProductCategoryData: MessageFns<ProductCategoryData> = {
  encode(message: ProductCategoryData, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.parentCategory !== undefined) {
      ProductCategoryData.encode(message.parentCategory, writer.uint32(34).fork()).join();
    }
    if (message.parentCategoryId !== undefined) {
      writer.uint32(42).string(message.parentCategoryId);
    }
    if (message.updatedAt !== undefined) {
      writer.uint32(50).string(message.updatedAt);
    }
    if (message.shopId !== "") {
      writer.uint32(58).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ProductCategoryData {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseProductCategoryData();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parentCategory = ProductCategoryData.decode(reader, reader.uint32());
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.parentCategoryId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.updatedAt = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ProductCategoryData {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      status: isSet(object.status) ? activeStatusFromJSON(object.status) : 0,
      parentCategory: isSet(object.parentCategory) ? ProductCategoryData.fromJSON(object.parentCategory) : undefined,
      parentCategoryId: isSet(object.parentCategoryId) ? globalThis.String(object.parentCategoryId) : undefined,
      updatedAt: isSet(object.updatedAt) ? globalThis.String(object.updatedAt) : undefined,
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
    };
  },

  toJSON(message: ProductCategoryData): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.status !== 0) {
      obj.status = activeStatusToJSON(message.status);
    }
    if (message.parentCategory !== undefined) {
      obj.parentCategory = ProductCategoryData.toJSON(message.parentCategory);
    }
    if (message.parentCategoryId !== undefined) {
      obj.parentCategoryId = message.parentCategoryId;
    }
    if (message.updatedAt !== undefined) {
      obj.updatedAt = message.updatedAt;
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ProductCategoryData>, I>>(base?: I): ProductCategoryData {
    return ProductCategoryData.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ProductCategoryData>, I>>(object: I): ProductCategoryData {
    const message = createBaseProductCategoryData();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.status = object.status ?? 0;
    message.parentCategory = (object.parentCategory !== undefined && object.parentCategory !== null)
      ? ProductCategoryData.fromPartial(object.parentCategory)
      : undefined;
    message.parentCategoryId = object.parentCategoryId ?? undefined;
    message.updatedAt = object.updatedAt ?? undefined;
    message.shopId = object.shopId ?? "";
    return message;
  },
};

function createBaseCreateProductCategoryRequest(): CreateProductCategoryRequest {
  return { shopId: "", name: "", status: 0, parentCategoryId: undefined };
}

export const CreateProductCategoryRequest: MessageFns<CreateProductCategoryRequest> = {
  encode(message: CreateProductCategoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopId !== "") {
      writer.uint32(10).string(message.shopId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.status !== 0) {
      writer.uint32(24).int32(message.status);
    }
    if (message.parentCategoryId !== undefined) {
      writer.uint32(34).string(message.parentCategoryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductCategoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductCategoryRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.status = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.parentCategoryId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductCategoryRequest {
    return {
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      status: isSet(object.status) ? activeStatusFromJSON(object.status) : 0,
      parentCategoryId: isSet(object.parentCategoryId) ? globalThis.String(object.parentCategoryId) : undefined,
    };
  },

  toJSON(message: CreateProductCategoryRequest): unknown {
    const obj: any = {};
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.status !== 0) {
      obj.status = activeStatusToJSON(message.status);
    }
    if (message.parentCategoryId !== undefined) {
      obj.parentCategoryId = message.parentCategoryId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductCategoryRequest>, I>>(base?: I): CreateProductCategoryRequest {
    return CreateProductCategoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductCategoryRequest>, I>>(object: I): CreateProductCategoryRequest {
    const message = createBaseCreateProductCategoryRequest();
    message.shopId = object.shopId ?? "";
    message.name = object.name ?? "";
    message.status = object.status ?? 0;
    message.parentCategoryId = object.parentCategoryId ?? undefined;
    return message;
  },
};

function createBaseCreateProductCategoryResponse(): CreateProductCategoryResponse {
  return { categoryId: "" };
}

export const CreateProductCategoryResponse: MessageFns<CreateProductCategoryResponse> = {
  encode(message: CreateProductCategoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.categoryId !== "") {
      writer.uint32(10).string(message.categoryId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateProductCategoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateProductCategoryResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.categoryId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateProductCategoryResponse {
    return { categoryId: isSet(object.categoryId) ? globalThis.String(object.categoryId) : "" };
  },

  toJSON(message: CreateProductCategoryResponse): unknown {
    const obj: any = {};
    if (message.categoryId !== "") {
      obj.categoryId = message.categoryId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateProductCategoryResponse>, I>>(base?: I): CreateProductCategoryResponse {
    return CreateProductCategoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateProductCategoryResponse>, I>>(
    object: I,
  ): CreateProductCategoryResponse {
    const message = createBaseCreateProductCategoryResponse();
    message.categoryId = object.categoryId ?? "";
    return message;
  },
};

function createBaseGetProductCategoriesRequest(): GetProductCategoriesRequest {
  return { search: "", sortBy: 0, desc: false, pageSize: 0, pageIndex: 0, status: [], shopId: "" };
}

export const GetProductCategoriesRequest: MessageFns<GetProductCategoriesRequest> = {
  encode(message: GetProductCategoriesRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.search !== "") {
      writer.uint32(10).string(message.search);
    }
    if (message.sortBy !== 0) {
      writer.uint32(16).int32(message.sortBy);
    }
    if (message.desc !== false) {
      writer.uint32(24).bool(message.desc);
    }
    if (message.pageSize !== 0) {
      writer.uint32(32).int32(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      writer.uint32(40).int32(message.pageIndex);
    }
    writer.uint32(50).fork();
    for (const v of message.status) {
      writer.int32(v);
    }
    writer.join();
    if (message.shopId !== "") {
      writer.uint32(58).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProductCategoriesRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProductCategoriesRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.search = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.sortBy = reader.int32() as any;
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.desc = reader.bool();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 6: {
          if (tag === 48) {
            message.status.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.status.push(reader.int32() as any);
            }

            continue;
          }

          break;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProductCategoriesRequest {
    return {
      search: isSet(object.search) ? globalThis.String(object.search) : "",
      sortBy: isSet(object.sortBy) ? productCategorySortByFromJSON(object.sortBy) : 0,
      desc: isSet(object.desc) ? globalThis.Boolean(object.desc) : false,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      status: globalThis.Array.isArray(object?.status) ? object.status.map((e: any) => activeStatusFromJSON(e)) : [],
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
    };
  },

  toJSON(message: GetProductCategoriesRequest): unknown {
    const obj: any = {};
    if (message.search !== "") {
      obj.search = message.search;
    }
    if (message.sortBy !== 0) {
      obj.sortBy = productCategorySortByToJSON(message.sortBy);
    }
    if (message.desc !== false) {
      obj.desc = message.desc;
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.status?.length) {
      obj.status = message.status.map((e) => activeStatusToJSON(e));
    }
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProductCategoriesRequest>, I>>(base?: I): GetProductCategoriesRequest {
    return GetProductCategoriesRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProductCategoriesRequest>, I>>(object: I): GetProductCategoriesRequest {
    const message = createBaseGetProductCategoriesRequest();
    message.search = object.search ?? "";
    message.sortBy = object.sortBy ?? 0;
    message.desc = object.desc ?? false;
    message.pageSize = object.pageSize ?? 0;
    message.pageIndex = object.pageIndex ?? 0;
    message.status = object.status?.map((e) => e) || [];
    message.shopId = object.shopId ?? "";
    return message;
  },
};

function createBaseGetProductCategoriesResponse(): GetProductCategoriesResponse {
  return { items: [], totalResults: 0, pageSize: 0, pageIndex: 0, maxPageIndex: 0 };
}

export const GetProductCategoriesResponse: MessageFns<GetProductCategoriesResponse> = {
  encode(message: GetProductCategoriesResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.items) {
      ProductCategoryData.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.totalResults !== 0) {
      writer.uint32(16).int64(message.totalResults);
    }
    if (message.pageSize !== 0) {
      writer.uint32(24).int32(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      writer.uint32(32).int32(message.pageIndex);
    }
    if (message.maxPageIndex !== 0) {
      writer.uint32(40).int32(message.maxPageIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetProductCategoriesResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetProductCategoriesResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.items.push(ProductCategoryData.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.totalResults = longToNumber(reader.int64());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.pageSize = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.pageIndex = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.maxPageIndex = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetProductCategoriesResponse {
    return {
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => ProductCategoryData.fromJSON(e))
        : [],
      totalResults: isSet(object.totalResults) ? globalThis.Number(object.totalResults) : 0,
      pageSize: isSet(object.pageSize) ? globalThis.Number(object.pageSize) : 0,
      pageIndex: isSet(object.pageIndex) ? globalThis.Number(object.pageIndex) : 0,
      maxPageIndex: isSet(object.maxPageIndex) ? globalThis.Number(object.maxPageIndex) : 0,
    };
  },

  toJSON(message: GetProductCategoriesResponse): unknown {
    const obj: any = {};
    if (message.items?.length) {
      obj.items = message.items.map((e) => ProductCategoryData.toJSON(e));
    }
    if (message.totalResults !== 0) {
      obj.totalResults = Math.round(message.totalResults);
    }
    if (message.pageSize !== 0) {
      obj.pageSize = Math.round(message.pageSize);
    }
    if (message.pageIndex !== 0) {
      obj.pageIndex = Math.round(message.pageIndex);
    }
    if (message.maxPageIndex !== 0) {
      obj.maxPageIndex = Math.round(message.maxPageIndex);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetProductCategoriesResponse>, I>>(base?: I): GetProductCategoriesResponse {
    return GetProductCategoriesResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetProductCategoriesResponse>, I>>(object: I): GetProductCategoriesResponse {
    const message = createBaseGetProductCategoriesResponse();
    message.items = object.items?.map((e) => ProductCategoryData.fromPartial(e)) || [];
    message.totalResults = object.totalResults ?? 0;
    message.pageSize = object.pageSize ?? 0;
    message.pageIndex = object.pageIndex ?? 0;
    message.maxPageIndex = object.maxPageIndex ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function longToNumber(int64: { toString(): string }): number {
  const num = globalThis.Number(int64.toString());
  if (num > globalThis.Number.MAX_SAFE_INTEGER) {
    throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
  }
  if (num < globalThis.Number.MIN_SAFE_INTEGER) {
    throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
  }
  return num;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
