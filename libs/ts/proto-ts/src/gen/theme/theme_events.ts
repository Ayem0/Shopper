// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: theme/theme_events.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface ThemeEvent {
  created?: ThemeCreated | undefined;
}

export interface ThemeCreated {
  shopId: string;
}

function createBaseThemeEvent(): ThemeEvent {
  return { created: undefined };
}

export const ThemeEvent: MessageFns<ThemeEvent> = {
  encode(message: ThemeEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.created !== undefined) {
      ThemeCreated.encode(message.created, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThemeEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThemeEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.created = ThemeCreated.decode(reader, reader.uint32());
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThemeEvent {
    return { created: isSet(object.created) ? ThemeCreated.fromJSON(object.created) : undefined };
  },

  toJSON(message: ThemeEvent): unknown {
    const obj: any = {};
    if (message.created !== undefined) {
      obj.created = ThemeCreated.toJSON(message.created);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThemeEvent>, I>>(base?: I): ThemeEvent {
    return ThemeEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThemeEvent>, I>>(object: I): ThemeEvent {
    const message = createBaseThemeEvent();
    message.created = (object.created !== undefined && object.created !== null)
      ? ThemeCreated.fromPartial(object.created)
      : undefined;
    return message;
  },
};

function createBaseThemeCreated(): ThemeCreated {
  return { shopId: "" };
}

export const ThemeCreated: MessageFns<ThemeCreated> = {
  encode(message: ThemeCreated, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopId !== "") {
      writer.uint32(10).string(message.shopId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThemeCreated {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThemeCreated();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThemeCreated {
    return { shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "" };
  },

  toJSON(message: ThemeCreated): unknown {
    const obj: any = {};
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ThemeCreated>, I>>(base?: I): ThemeCreated {
    return ThemeCreated.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ThemeCreated>, I>>(object: I): ThemeCreated {
    const message = createBaseThemeCreated();
    message.shopId = object.shopId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
