// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.6.1
//   protoc               unknown
// source: theme/theme_types.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export interface UpdateThemeRequest {
  shopId: string;
  styles: string;
}

export interface UpdateThemeResponse {
  styles: string;
}

function createBaseUpdateThemeRequest(): UpdateThemeRequest {
  return { shopId: "", styles: "" };
}

export const UpdateThemeRequest: MessageFns<UpdateThemeRequest> = {
  encode(message: UpdateThemeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.shopId !== "") {
      writer.uint32(10).string(message.shopId);
    }
    if (message.styles !== "") {
      writer.uint32(18).string(message.styles);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateThemeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateThemeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.shopId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.styles = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateThemeRequest {
    return {
      shopId: isSet(object.shopId) ? globalThis.String(object.shopId) : "",
      styles: isSet(object.styles) ? globalThis.String(object.styles) : "",
    };
  },

  toJSON(message: UpdateThemeRequest): unknown {
    const obj: any = {};
    if (message.shopId !== "") {
      obj.shopId = message.shopId;
    }
    if (message.styles !== "") {
      obj.styles = message.styles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateThemeRequest>, I>>(base?: I): UpdateThemeRequest {
    return UpdateThemeRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateThemeRequest>, I>>(object: I): UpdateThemeRequest {
    const message = createBaseUpdateThemeRequest();
    message.shopId = object.shopId ?? "";
    message.styles = object.styles ?? "";
    return message;
  },
};

function createBaseUpdateThemeResponse(): UpdateThemeResponse {
  return { styles: "" };
}

export const UpdateThemeResponse: MessageFns<UpdateThemeResponse> = {
  encode(message: UpdateThemeResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.styles !== "") {
      writer.uint32(10).string(message.styles);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UpdateThemeResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUpdateThemeResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.styles = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UpdateThemeResponse {
    return { styles: isSet(object.styles) ? globalThis.String(object.styles) : "" };
  },

  toJSON(message: UpdateThemeResponse): unknown {
    const obj: any = {};
    if (message.styles !== "") {
      obj.styles = message.styles;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UpdateThemeResponse>, I>>(base?: I): UpdateThemeResponse {
    return UpdateThemeResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UpdateThemeResponse>, I>>(object: I): UpdateThemeResponse {
    const message = createBaseUpdateThemeResponse();
    message.styles = object.styles ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
